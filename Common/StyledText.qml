import QtQuick
import qs.Config

Text {
    color:          Colors.col_main
    font.family:    Settings.font_main
    font.pixelSize: Settings.font_size_normal
    renderType:     Text.NativeRendering
}